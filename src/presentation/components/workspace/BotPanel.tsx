import React from 'react';
import { Box } from '../common/Box';

export const BotPanel: React.FC = () => {
  return (
    <Box title="Bot e IA">
      <p>Modo sugerir respuesta. Proveedor configurable via backend.</p>
    </Box>
  );
};